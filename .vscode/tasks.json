{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "local-data-debug",
            "command": "docker compose up -d --build --remove-orphans app-local-debug",
            "type": "shell"
        },
        {
            "label": "local-data",
            "command": "docker compose up -d --build --remove-orphans app-local",
            "type": "shell"
        }
    ]
}